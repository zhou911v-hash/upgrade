<UserControl x:Class="SmartBuildingLighting.App.Controls.PageHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <Grid Margin="0,0,0,20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <!-- 英文代号 + 分隔条 -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <Ellipse Style="{StaticResource StatusDotAccent}" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding Code, ElementName=Root}"
                           FontSize="10" FontWeight="Bold"
                           Foreground="{StaticResource AccentBrush}"
                           FontFamily="{StaticResource MonoFontFamily}"
                           VerticalAlignment="Center" Margin="10,0,0,0"/>
                <Border Width="100" Height="1" Background="{StaticResource PrimaryLightBrush}"
                        VerticalAlignment="Center" Margin="12,0,0,0"/>
            </StackPanel>
            <!-- 中文大标题 -->
            <StackPanel Orientation="Horizontal">
                <Border Width="3" Height="24" Background="{StaticResource AccentBrush}" VerticalAlignment="Center">
                    <Border.Effect>
                        <DropShadowEffect Color="#00E5FF" BlurRadius="8" ShadowDepth="0" Opacity="0.6"/>
                    </Border.Effect>
                </Border>
                <TextBlock Text="{Binding Title, ElementName=Root}"
                           Style="{StaticResource PageTitle}"
                           Margin="12,0,0,0"
                           VerticalAlignment="Center"/>
            </StackPanel>
            <!-- 副标题（英文或说明） -->
            <TextBlock Text="{Binding Subtitle, ElementName=Root}"
                       Style="{StaticResource BodyText}"
                       Margin="15,6,0,0"
                       MaxWidth="820"
                       TextWrapping="Wrap"
                       Foreground="{StaticResource TextMutedBrush}"
                       Visibility="{Binding Subtitle, ElementName=Root, Converter={StaticResource NullToVisibility}}"/>
        </StackPanel>
        <ContentPresenter Grid.Column="1"
                          Content="{Binding HeaderActions, ElementName=Root}"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Right"/>
    </Grid>
</UserControl>
